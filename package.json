{
  "name": "@nezha/nupi",
  "version": "0.5.0",
  "description": "NuPI = Nezha (core) + Pi (local executor). Hooks bridge between Pi agent and Nezha data layer",
  "type": "module",
  "main": "./dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc && node scripts/replace-hash.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node bin/nupi",
    "predev": "npm run build",
    "dev": "rm -rf $(npm root -g)/@nezha/nupi; npm link; echo === STATUS ===; echo Local:; grep GIT_HASH dist/extension.js; echo Global:; grep GIT_HASH $(npm root -g)/@nezha/nupi/dist/extension.js",
    "prestatus": "test -d dist || npm run build",
    "status": "echo === STATUS ===; echo Local:; grep GIT_HASH dist/extension.js; echo Global:; grep GIT_HASH $(npm root -g)/@nezha/nupi/dist/extension.js"
  },
  "bin": {
    "nupi": "./bin/nupi"
  },
  "keywords": [
    "nezha",
    "ai",
    "pi",
    "execution"
  ],
  "author": "emptist",
  "license": "MIT",
  "dependencies": {
    "pg": "^8.20.0"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": ">=0.63.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/pg": "^8.10.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/emptist/nupi.git"
  },
  "bugs": {
    "url": "https://github.com/emptist/nezha/issues"
  }
}
